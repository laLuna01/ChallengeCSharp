@model ChallengeCSharp.Web.Models.DentistaViewModel

@{
    ViewData["Title"] = "Editar Dentista";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <h2 class="text-primary mb-4">@ViewData["Title"]</h2>

            <form asp-action="Edit" method="post" class="needs-validation" novalidate>
                <input type="hidden" asp-for="IdDentista" />

                <div class="mb-3">
                    <label asp-for="Nome" class="form-label">Nome</label>
                    <input asp-for="Nome" class="form-control" placeholder="Ex: São Paulo, California..." />
                    <span asp-validation-for="Nome" class="text-danger small"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Especialidade" class="form-label">Especialidade</label>
                    <input asp-for="Especialidade" class="form-control" placeholder="Ex: São Paulo, California..." />
                    <span asp-validation-for="Especialidade" class="text-danger small"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="CRO" class="form-label">CRO</label>
                    <input asp-for="CRO" class="form-control" placeholder="Ex: São Paulo, California..." />
                    <span asp-validation-for="CRO" class="text-danger small"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="IdGenero" class="form-label">Gênero</label>
                    <select asp-for="IdGenero" asp-items="Model.Generos" class="form-select">
                        <option value="">-- Selecione um gênero --</option>
                    </select>
                    <span asp-validation-for="IdGenero" class="text-danger small"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="IdEndereco" class="form-label">Endereço</label>
                    <select asp-for="IdEndereco" asp-items="Model.Enderecos" class="form-select">
                        <option value="">-- Selecione um endereço --</option>
                    </select>
                    <span asp-validation-for="IdEndereco" class="text-danger small"></span>
                </div>

                <div class="d-flex justify-content-between">
                    <a asp-action="Index" class="btn btn-outline-secondary">
                        <i class="bi bi-arrow-left-circle me-1"></i> Voltar
                    </a>
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-save me-1"></i> Salvar Alterações
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}